From 6c10f8cfc85b1ef64f8f965942091ad0190e90c2 Mon Sep 17 00:00:00 2001
From: julien delnatte <julien.delnatte@gmail.com>
Date: Sun, 27 Jun 2021 18:48:33 +0200
Subject: [PATCH] remove generated by and processes

---
 src/mode/fdm/export.js | 18 +++++++++---------
 1 file changed, 9 insertions(+), 9 deletions(-)

diff --git a/src/mode/fdm/export.js b/src/mode/fdm/export.js
index d52ba62a..b5454301 100644
--- a/src/mode/fdm/export.js
+++ b/src/mode/fdm/export.js
@@ -199,15 +199,15 @@
             arr.forEach(function(line) { appendSub(line, pad) });
         }
 
-        append(`; Generated by Kiri:Moto ${KIRI.version}`);
-        append(`; ${new Date().toString()}`);
-        appendSub("; Bed left:{left} right:{right} top:{top} bottom:{bottom}");
-        append(`; Bed type: ${bedType}`);
-        append(`; Target: ${settings.filter[settings.mode]}`);
-        append("; --- process ---");
-        for (let pk in process) {
-            append("; " + pk + " = " + process[pk]);
-        }
+        // append(`; Generated by Kiri:Moto ${KIRI.version}`);
+        // append(`; ${new Date().toString()}`);
+        // appendSub("; Bed left:{left} right:{right} top:{top} bottom:{bottom}");
+        // append(`; Bed type: ${bedType}`);
+        // append(`; Target: ${settings.filter[settings.mode]}`);
+        // append("; --- process ---");
+        // for (let pk in process) {
+        //     append("; " + pk + " = " + process[pk]);
+        // }
         append("; --- startup ---");
         let t0 = false;
         let t1 = false;
-- 
2.20.1 (Apple Git-117)

